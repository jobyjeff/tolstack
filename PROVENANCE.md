# PROVENANCE — what was imported at tolstack's founding

tolstack was stamped from forge's `template/` on 2026-08-03 (root commit
`e7bd996`, `forge new-repo`). Its tolerance-stack material was **not** written
here: it was imported from **drawing-checker**'s `tolerance_stack_slice1` slice
(handoff 2026-07-29, worked 2026-07-30) by handoff `tolstack_founding`.

| | |
|---|---|
| Source repo | `C:\workspace\drawing-checker` |
| Source repo `master` at time of copy | **`0743640dc5d41fe25d7084694572b29265fe632b`** (`0743640`, 2026-08-04) |
| Commits that created the material there | `2bb9648` (shapes, fold module, 34 tests, 4 debug tools, inbox copy) and `720df27` (README + both worksheets) |
| Imported by | handoff `tolstack_founding`, 2026-08-03 |
| Import commit in this repo | **`c157300`** — "Import the tolerance-stack assets from drawing-checker (34 tests green)". The in-repo baseline every byte-identical row is diffed against by `tests/test_provenance.py`; the row above is the *source* baseline. Both are git history and do not move |

## The rule

Everything below except the specs pile was **copied**: the originals stay in
drawing-checker, untouched. That is the append-only spirit — an import is
one-way and additive, and no drawing-checker file was edited or deleted to make
this repo exist. Only `data/inbox/specs/` was **moved**, because nothing in
drawing-checker had ever consumed it.

Where a copied file was amended here, the amendment is listed in the Amended
column. At founding every amendment was limited to an "imported"/"where this
lives" note. Three files change substantively because the repo's own SOP requires
it — `hardware_entries.json` (Step 4), `tests/test_tolerance_stack.py` (Step 7)
and `docs/tolerance_stacks/README.md` grow with every new stack. **A
"byte-identical" row is a claim about the import, not a freeze**; when one of
these files changes, amend its row in the same commit. The rows below say when
each stopped being byte-identical and why — count them there rather than trusting
a number in this paragraph, because those three are only the *predictable* ones.
The rows nobody watches are where this record has actually gone false: the
package files (`__init__.py`, `spec_library_v0`), the seeded stack JSONs and
worksheets (`traced_labels_and_ratio`), and the imported reference lesson (same).
Every one of those was caught by a reviewer rather than by the author, and each
time the author had changed the file for a good reason and simply not looked here.
**Before you finish any handoff: `git diff master..HEAD --name-only` against this
table.**

**Since 2026-08-10 you do not have to remember to** — `tests/test_provenance.py`
runs that diff for you and fails naming the row and what to write, so the table
is checked by the suite rather than by whoever thinks to look. It parses the
tables below, so **the Amended column's wording is load-bearing**: a cell must
begin `no` (still what was imported), `yes` (changed — say when and why) or
`not imported` (authored here). It also verifies the byte-identical rows against
drawing-checker's blobs at the sha above, which was previously a manual
`sha256sum` step in the review checklist. The five sightings are replayed out of
git as regression cases in that file.

**And it worked the first time it was asked to, 2026-08-10.** The very next
handoff (`fastener_citations_and_confidence`) changed two seeded stack JSONs, two
worksheets, `hardware_entries.json` and `test_tolerance_stack.py` — six rows,
including `stack_tan_link_to_pitch_plate_take2.json`, which had claimed
byte-identity through four handoffs. The suite went red on the author's own
machine, named all six rows and said what to append. So the sentence above —
*"every one of those was caught by a reviewer rather than by the author"* — has
its first counterexample, and the reviewer's job on this table is now the one
thing the test cannot do: read whether an amendment is **true**. One consequence
worth recording, because it is the number that moves: **`tests/__init__.py` is
now the only row in this document still claiming byte-identity** (it is empty).
`take2`'s row was the other one, and it was byte-identical for exactly as long
as nothing in it needed correcting.

## Copied — `docs/tolerance_stacks/`

Design artifacts, committed (not run data).

| source (drawing-checker) | destination (tolstack) | amended |
|---|---|---|
| `docs/tolerance_stacks/stack_tan_link_to_pitch_plate.json` | same path | **yes, since 2026-08-06** — was byte-identical at import, and **two sibling handoffs changed it on the same day**, each caught by its own reviewer still claiming byte-identical. (1) `citation_export_provenance`, **additive only:** a `source_ref.export` block on each of the four `drawing`/`parts_list` citations, naming the export the value was read from by `sha256` (three on the 2026-JUL-23 POST export of 217755, one on the 215197 fixture). No value, band, sign, `confidence` or `callout` changed; the stack re-derives identically. Row added during `review/citation_export_provenance`. (2) `traced_labels_and_ratio`, **`source_ref` and `note` only; no numeric field, path or check changed** (verified by stripping `source_ref`/`note` and diffing against `master`: structurally identical, and the re-derivation table's largest delta unchanged at 6.439e-15): `fastener_grip_14` re-cited from the 217755 parts list to `NAS6403-NAS6420 Rev 4.pdf` sheet 3 (`kind: parts_list` → `spec`), staying `traced` — legitimately this time, since the band is the sheet-3 column header `Grip ±.010` — plus two stale `(NAS6403 spec absent)` parentheticals corrected on `thread_transition` and `fastener_grip_13`. Row amended during `review/traced_labels_and_ratio`. **The two met for the first time in that review's merge commit**, where `fastener_grip_14` became `kind: "spec"` and so left the set of citations `citation_export_provenance` requires an `export` on — the one interaction between them, and one neither branch's suite could see. **Amended again 2026-08-07** (`readonly_invariant_evidence`), **additive and mechanical:** the 11 entries in this file's three `export.runs` lists went from bare run ids to `{"run_id": ..., "ts": ...}`, each `ts` copied verbatim from that run's own `run_meta.json` in drawing-checker (read-only). No value, band, sign, `confidence`, `callout`, `sha256` or `pdf` changed and no run was added or dropped; the diff is exactly those 11 lines. **Amended again 2026-08-10** (`fastener_citations_and_confidence`), **`source_ref` and `note` only; no numeric field, path or check changed** (verified the same way as the 08-06 entry above — stripping `source_ref`/`note` and diffing the structures against `HEAD`: identical — and independently by diffing the full `debug_report_tolerance_stacks.py` output before and after, where every check row is byte-for-byte the same): `fastener_grip_13` re-cited from the 217755 parts list to `NAS6403-NAS6420 Rev 4.pdf` sheet 3 row *Grip Dash No. 13* (`kind: parts_list` → `spec`, `inferred` → `traced`), which also **drops its `source_ref.export` block** — the same correct drop `fastener_grip_14` made on 08-06, since `kind: "spec"` is exempt from the export requirement; the block's `sha256` is still live on this file's `straight_bushing` and in git history. `washer_thin` **downgraded `inferred` → `untraced`** (its only support for `.032 ±.004` is workbook cell E11, which its own note already said). `thread_transition` **decided and left unchanged** at `kind: assumed` / `untraced` / 1.5875 mm, with the reasoning written into its note |
| `docs/tolerance_stacks/stack_tan_link_to_pitch_plate_take2.json` | same path | **yes, since 2026-08-10** (`fastener_citations_and_confidence`) — was byte-identical at import and stayed so through four handoffs, because every citation in it was `kind: "workbook"`: `citation_export_provenance` had nothing to add and `traced_labels_and_ratio` relabelled nothing here. **This is the first branch to change it, and it changed it precisely because those `kind: "workbook"` citations were the problem.** **`source_ref` and `note` only; no numeric field, path or check changed** (verified by stripping `source_ref`/`note` and diffing the structures against `HEAD`: identical; and the stack's one check, `worst_case_protrusion`, prints the same six figures before and after): `fastener_grip_13` re-cited from workbook cell E52 to `NAS6403-NAS6420 Rev 4.pdf` sheet 3 (`kind: workbook` → `spec`, `inferred` → `traced`) — it is the same bolt as take 1's, which was cited to a different document, so the two stacks of one joint disagreed about where the number came from. `straight_bushing` **downgraded `inferred` → `untraced`**, its only support being workbook literals with no inch source. Note for the next reader: this row went from `no` to `yes`, which is the transition `tests/test_provenance.py` exists to catch, and it caught it |
| `docs/tolerance_stacks/stack_vpa_output_to_pitch_plate.json` | same path | **yes, since 2026-08-06** — same story as the tan-link file above, and the same two handoffs. (1) `citation_export_provenance`, **additive only:** a `source_ref.export` block on each of the three `drawing`/`parts_list` citations (two on the 2026-JUL-23 POST export of 217755, one on the 215197 fixture); no value, band, sign, `confidence` or `callout` changed. Row added during `review/citation_export_provenance`. (2) `traced_labels_and_ratio`, **`source_ref` and `note` only, no numeric field changed** (verified the same way): `fastener_grip` re-cited to `NAS6403-NAS6420 Rev 4.pdf` sheet 3, NAS6404 column, staying `traced`; `under_head_chamfer_washer` **downgraded `traced` → `inferred`**, because MS21299 is not in `data/inbox/specs/` and a parts list gives a nominal and never a band. Row amended during `review/traced_labels_and_ratio`. **Amended again 2026-08-07** (`readonly_invariant_evidence`), **additive and mechanical:** the 7 entries in this file's two `export.runs` lists gained their `run_meta.json` `ts`, same shape and same non-effect as the tan-link row above. **Amended again 2026-08-13** (`spec_pile_gap_join`), **one `source_ref.note` and nothing else — no value, band, sign, `confidence`, `callout`, path or check changed** (verified by stripping `source_ref`/`note` and diffing the structures against `master`: identical, and by diffing the full `debug_report_tolerance_stacks.py` output before and after, which is byte-for-byte the same): `straight_bushing`'s note records the 2026-08-13 reading of NAS77, which was in `data/inbox/specs/` all along and **does not source this element** — NAS77 is the unlined FLANGED series (NAS76 is the straight one), the dash decode printed on the page makes `NAS77A4-015` .150 in long against the .1875 in folded here, and the printed length tolerance is `L ±.005 in`, not the ±.002 in that 4.71/4.81 mm implies. The element **stays** `kind: workbook` / `untraced` with its value unchanged; 214943-002's part drawing is still what closes it. The decision is registered in `tests/debug_report_spec_pile_gaps.py`'s `KNOWN_NON_MATCHES` so the join stops offering it. **Amended once more during `review/spec_pile_gap_join`**, page addresses only — **no value, band, sign, `confidence`, `callout`, `kind` or decision changed**: the note cited NAS77 at "page 92 of both RBC plain-bearing catalogues" and NAS76 at "page 91", which is right for `RBC - Plain bearings (NAS77 p92).pdf` (© 2008/2011/2016) and wrong for `RBC_Aerospace_Plain_Bearings_Web.pdf` (© 2008), where the same two tables are printed pages **97** and **98**. The pages are now named by their table rather than by a number borrowed from the sibling edition |
| `docs/tolerance_stacks/hardware_entries.json` | same path | **yes, since 2026-08-04** (`pitch_link_stack`) — was byte-identical at import. Added the `NAS6403U11D` entry (the first whose inline values are traced to a standard rather than to the 260729 workbook, carrying a proposed additive `values_source` field), extended four entries' `gaps`, and added `used_by` back-references. SOP Step 4 requires a hardware entry per standard part, so this file changes with every new stack. **Amended again 2026-08-05** (`sop_edits_apply`): `values_source` backfilled to all 13 entries — eight `kind: "workbook"` / `untraced`, four explicit `null` for the not-transcribed entries — the `sheets` key on `NAS6403U11D` renamed to `sheet` to match `SourceRef`, and the file `description` corrected (it still claimed no entry came from a standard document). **Amended again 2026-08-05** (`spec_library_v0`): `NAS6403U11D` promoted to `values_status: "library"` with `library_ref: "spec_library:NAS6403U11D"` and a `library_ref_note`, and its dimension-`M` gap closed by the second reader. The other twelve entries untouched by that handoff. **Amended again 2026-08-05** (`hub_bearing_thermal_stack`): the two NSK bearings `214589-002` / `214588-002` added -- the first entries whose every inline value is traced to a **drawing face** (a source-control drawing is the controlling document for a vendor part), and the first with `assembly_status.present: null` meaning *not checked* rather than *absent*. The `description`'s value-source counts were also corrected and are now asserted by a test, because that sentence had already gone stale once. **15 entries**. **Amended again 2026-08-10** (`fastener_citations_and_confidence`), **`values_source`, `gaps` and `description` only — no `dimensions_in`, `dimensions_mm`, `assembly_status`, `used_by` or `library_ref` field changed, and no entry was added or removed (still 15)**: `NAS6403U13H`, `NAS6403U14D` and `NAS6404U13D` re-sourced from `kind: "workbook"` / `untraced` to `kind: "spec"` / `traced` on `NAS6403-NAS6420 Rev 4.pdf` sheet 3, which prints both their grips and the ±.010 band. Two of those three had disagreed with the stack elements they serve since 2026-08-06; all three carried `gaps` asserting the NAS6403/NAS6404 standard was "absent from this repo", which had been false since that PDF landed. Those gap entries are rewritten as dated `CLOSED` lines with the true remaining gap (the thread run-out, which NAS6403 does **not** dimension — MIL-S-8879 closes it) rather than deleted. The `description`'s counts move with them: *eight* of the fifteen entries transcribing the workbook → **five**, *THREE* traced → **SIX**; the four `not_transcribed` are unchanged. A stale count inside `NAS6403U11D`'s `library_ref_note` ("The other twelve entries") was also corrected — it was written against a 13-entry file. **Amended again 2026-08-13** (`spec_pile_gap_join`), **`gaps`, one `values_source.note` and one `description` clause only — no `values_status`, `library_ref`, `values_source.kind`/`confidence`, `dimensions_in`, `dimensions_mm`, `assembly_status` or `used_by` field changed, no entry added or removed (still 15), and every count this file states is unmoved**: four entries' gap prose was stale and two of the four were making false claims. `MS9363-09`'s 2026-08-04 clause *"What is still missing is entirely on the nut side: NUT HEIGHT, slot count, and slot depth"* had been false since `MS9363 Rev C.pdf` arrived on 2026-08-05 — sheet 1 TABLE I row `-09` prints `H`, `G`, `S` and `6 PLACES`, re-read off a crop before this correction — and it was hiding the more interesting finding, that the quantity the joint actually needs (the thread-start-to-castellation phase) is **uncontrolled by MS9363 and by everything else**, so it is not an acquisition gap at all. `MS9363-10` gains the matching closure on row `-10`. `NAS77A4-015`'s *"NAS77 standard absent"* was false since founding: NAS77 is in the pile three times over, and reading it **falsifies the entry's part number** rather than sourcing it (flanged series, .150 in long, `L ±.005 in`), so the values stay untraced and unchanged and its `values_source.note` records the attempt as declined. `MS24665-229`'s *"Same, for the VPA joint"* gains the closure its sibling got on 2026-08-04, off the NAS6404 row (`M = .180/.160 in`, `P = .086/.076 in`). The `description`'s closing clause — *"nothing in the repo noticed that a new document had made an existing gap closable"* — is now answered in place: `tests/debug_report_spec_pile_gaps.py` does the noticing. **Amended once more during `review/spec_pile_gap_join`**, two page addresses inside `NAS77A4-015`'s corrected gap and nothing else — **no `values_status`, `values_source`, `dimensions_*`, count or other entry touched**: the gap read "pages 92 of both RBC plain-bearing catalogues" (and NAS76 at "p91"), true of the 2016 reprint and false of the 2008 web edition, where those tables are printed pages 98 and 97. `JB_NAS77.pdf` is a one-page extract of that 2008 p98, which is what made the two look interchangeable. **Amended again 2026-08-25** (`fastener_stack_shadow`) -- ten entries added for the rotor balance-mass bolt (217755 sheet 8 SECTION T-T): `NAS6403U2H` through `NAS6403U10H`, the grip-selection family's other nine members, traced to the same sheet-3 table as `NAS6403U11D`/`U13H`/`U14D`; and `MS21299C3`, a new washer entry (`kind: parts_list`, `inferred`, MS21299 still absent from the pile). `used_by` on the existing `NAS1149V0332` entry gained a third reference (`rotor_fastener_length:washer_nas1149v0332_tt`), the same washer at a different balloon of the same drawing. **25 entries.** The `description`'s counts moved with them (five of 25 workbook, FIFTEEN traced, four `not_transcribed` unchanged) and so did the stale count inside `NAS6403U11D`'s `library_ref_note` ("the other fourteen do not... ten inline, four not_transcribed" -> "the other 24 do not... twenty inline, four not_transcribed") |
| `docs/tolerance_stacks/WORKSHEET_tan_link_to_pitch_plate.md` | same path | **yes, since 2026-08-06** (`traced_labels_and_ratio`) — was byte-identical at import. A dated "Provenance update" note above the element table, element 11's source column re-pointed at `NAS6403-NAS6420 Rev 4.pdf`, gap 1 marked partially closed, and the ratio section restated (this stack: 2 traced / 3 inferred / 6 untraced of 11) with a dated correction quoting the old *"one element traced out of eleven"*. **No results, fold, RSS or re-derivation table changed** — the relabelling touches no arithmetic. Row amended during `review/traced_labels_and_ratio`. **Amended again 2026-08-10** (`fastener_citations_and_confidence`), same shape and again **no results, fold, RSS or re-derivation table changed**: a second dated "Provenance update" note above the element table covering elements 7, 9 and 10; element 10's source/conf columns re-pointed at `NAS6403-NAS6420 Rev 4.pdf` sheet 3 and element 7's conf column changed `inferred` → `untraced`; source gaps 1 and 3 rewritten (gap 1 closed for grip and re-scoped to the thread run-out that NAS6403 does not give, gap 3 pointing at the now-`untraced` thin washer); and the count section restated — this stack 3 traced / 1 inferred / 7 untraced of 11, take 2 (which this worksheet also covers and which has no element table) 1 / 0 / 8 of 9 — with a dated "Moved" blockquote quoting the superseded per-stack figures. **Amended once more during `review/fastener_citations_and_confidence`**, one blockquote and nothing else: the castellation/cotter caveat that review checklist 6 requires *next to the numbers* was repeated under the Checks table, where it had lived only at finding F8 some 150 lines below. **No element, path, check, result, RSS figure or provenance label changed**. **Amended again 2026-08-13** (`spec_pile_gap_join`), **one row of the source-gap table and nothing else — no element, path, check, result, RSS figure, ratio or provenance label changed**: gap row 2 said the repo still needed **MS9363** for castellation slot count and depth. `MS9363 Rev C.pdf` had been in `data/inbox/specs/` since 2026-08-05, the day after this worksheet's session, and prints all of it on sheet 1 TABLE I row `-09`. The row is struck through and annotated rather than deleted, and it now carries the sharper half: the check that governs this joint needs the thread-start-to-castellation **phase**, which MS9363 requirement 10 does not control and no document supplies, so it was never an intake item — the remedy is JPS00094 §5.9.7's assembly-time selection |
| `docs/tolerance_stacks/WORKSHEET_vpa_output_to_pitch_plate.md` | same path | **yes, since 2026-08-06** (`traced_labels_and_ratio`) — was byte-identical at import. Same shape of change: a dated provenance update, elements 5 and 6 re-labelled in the source/conf columns, gaps 1 and 3 annotated, and the ratio section restated (1 traced / 2 inferred / 3 untraced of 6) with a dated correction. **No results or re-derivation table changed.** Row amended during `review/traced_labels_and_ratio`. **Amended again 2026-08-10** (`fastener_citations_and_confidence`), **one paragraph and one added blockquote, nothing else**: the three-stack ratio quoted in the count section moved 3 of 26 → 5 of 26 because the two tan-link stacks changed. **Nothing on this stack changed at all** — no element, no conf column, no gap, no result. The added blockquote says so explicitly and records the one thing near this stack that did move, in `hardware_entries.json`: `NAS6404U13D`'s `values_source`. **Amended again 2026-08-13** (`spec_pile_gap_join`), **two source-gap rows and one added blockquote under F13 — no element, path, check, result, RSS figure, ratio or provenance label changed, and nothing on this stack moved**: gap row 2 still asked for **MS9363-10**, which has been in the pile since 2026-08-05 and prints the `-10` nut's height and slot geometry on sheet 1 (identical to the `-09`, the dash carrying only the thread size); F16 stays open for the uncontrolled castellation phase, which is not an acquisition. Gap row 7 asked whether the workbook's **NAS77** part exists in the design, and that document was in the pile all along — read 2026-08-13, it answers in the negative: NAS77 is the FLANGED series, the as-drawn part is a plain bushing, and the dash decode printed on the page makes `NAS77A4-015` .150 in long where this stack folds .1875 in. The F13 blockquote records that reading and the matching non-decision on element 1, whose value and `untraced` label are **unchanged**. **Amended once more during `review/spec_pile_gap_join`**, the same page-address correction as the stack file's — **no element, path, check, result, RSS figure, ratio, gap row or provenance label changed**: the F13 blockquote and gap row 7 cited the NAS77/NAS76 tables as printed pages 92 and 91 of *both* RBC catalogues; they are 92/91 in the 2016 reprint and 98/97 in the 2008 web edition |
| `docs/tolerance_stacks/README.md` | same path | **yes** — its "Where this lives is NOT decided" section is now answered (it lives here); added pointers to the SOP and to `docs/reference/`, and corrected "three shapes" to four. **Amended again 2026-08-05** (`hub_bearing_thermal_stack`): "four shapes" corrected to five (`material_entry/v0`), a "two archetypes, not one kind of stack" section added, the six new files listed, and the regeneration block given the thermal reporter's four modes. **Amended again 2026-08-11** (`sop_library_ref_pairing`), **prose only — no file listed, no count and no numeric or schema fact changed**: the three places this file still said `hardware_entry`'s `library_ref` is an empty slot awaiting a library that does not exist. The library has existed since 2026-08-05, so the "still a separate open question" paragraph, the `hardware_entries.json` row of the Contents table, and the **hardware entry** shape paragraph now state the pairing invariant (a filled ref ⟺ `values_status == "library"`) and name the one promoted entry. A fourth, smaller edit in the **material entry** paragraph: its `library_ref: null` claim is still **true** (there is no materials library) and is kept, with two sentences added saying so and naming the test that asserts it, because "same discipline as `hardware_entry`" had stopped being true of exactly that field and the next reader would otherwise have "fixed" a correct line. The stale `values_source` counts two paragraphs down were **left alone deliberately** and filed as `docs/issues/ISSUE_20260811_the_values_source_counts_in_two_live_docs_are_stale.md` — they belong to `fastener_citations_and_confidence`'s number, not to this handoff's invariant. **Amended once more during `review/sop_library_ref_pairing`**, four words in the `hardware_entries.json` row and nothing else: the new prose had introduced a fresh unguarded count of its own (*"one entry so far"*), which is the very class the same handoff's lesson says it kept out — replaced with a pointer to `test_only_the_one_entry_was_promoted`, which owns that number. **No file listed, no schema fact and no other cell changed**. **Amended again 2026-08-12** (`hardware_counts_doc_guard`), **one paragraph, prose only — no file listed, no count, no schema fact and no other cell changed**: the stale `values_source` counts the note above left alone deliberately are gone, and nothing replaced them. The **hardware entry** paragraph's count sentence — *"Eight of the eleven inline entries say `kind: "workbook"`"*, true until 2026-08-10 and stale from then — now states **no count at all**: it says what each `kind` means for reuse and points at `hardware_entries.json`'s own `description` plus the two tests that own the numbers. Option 2 of `docs/issues/ISSUE_20260811_the_values_source_counts_in_two_live_docs_are_stale.md`, chosen because the sentence that went stale was itself the one telling the reader not to quote counts from here — a prose warning is not a guard. **Amended again 2026-08-13**, during `review/spec_pile_gap_join`, **one command and one paragraph appended to the Regenerating block — no file listed, no count, no schema fact and no other cell changed**: `tests\debug_report_spec_pile_gaps.py` was added by that handoff and named nowhere a future agent would look, so the operational fact (run it when a document lands in `data/inbox/specs/`, and before writing a gap that calls a standard absent; MAIN checkout only) lived only in the tool's own docstring and in a lesson. Every other `debug_report_*` tool is listed here; this one now is too. **Amended again 2026-08-17** (`material_cte_optional`), **two sentences inside the material entry paragraph — no file listed, no count and no other cell changed**: that paragraph said the shape's discipline was `values_status: inline` plus a mandatory `values_source`, and said nothing about the CTE, which was mandatory unconditionally. `MaterialEntry.cte_1e6_per_c` is now conditional exactly as `values_source` is (required unless `values_status` is `not_transcribed`), so the paragraph states that condition and what a chain naming a CTE-less material does — it refuses to load, naming the material, rather than folding against a zero CTE. **Amended again 2026-08-25** (`endstop_graft_workorder`), **one Contents-table row and one source-workbook sentence — no other cell changed**: added the `WORKSHEET_end_stop_graft.md` row (a third archetype, the blade-pitch angular-position-error rollup read from `260825_End_Stop_JC.xlsx`) and named that workbook alongside the other two in the source-workbooks sentence. **Amended again 2026-08-25** (`fastener_stack_shadow`), **Contents table only — no count, no schema fact and no other cell changed**: two rows added for `stack_rotor_fastener_length.json` and its worksheet, and the `hardware_entries.json` row's sentence extended to mention the new NAS6403 grip family and washer, in prose with no digit **Amended again 2026-09-01** (`endstop_vision_baseline`), **one Contents-table row and nothing else — no count, no schema fact, no source-workbook sentence and no other cell changed**: a row for `WORKSHEET_endstop_vision_baseline.md`, which is not a stack but a calibrated capability baseline (an attempt to derive the end-stop stack from the drawings alone, scored against `WORKSHEET_end_stop_graft.md`). The row states that attempt's traced figure (4 of 43) and its headline finding; both live in the worksheet, and the row points at it rather than restating anything the worksheet owns. |

## Copied — the `tolerance_stack/` package

| source | destination | amended |
|---|---|---|
| `tolerance_stack/__init__.py` | same path | **yes, since 2026-08-05** (`spec_library_v0`) — was byte-identical at import. Re-exports the thirteen `spec_library` public names alongside the existing `stack` ones, and extends `__all__`. Additive: no imported name changed or moved. Row corrected during `review/spec_library_v0`, which found it still claiming byte-identical — see the finding in that review report. **Amended again 2026-08-06** (`citation_export_provenance`): `SourceExport` re-exported and added to `__all__`. Additive again. **Amended again 2026-08-07** (`readonly_invariant_evidence`): `ExportRun` re-exported and added to `__all__`, both in alphabetical position. Additive again — and this row is the one the last two handoffs each nearly missed, so: **a new public dataclass means this file changes** — and the same is true of a new module **constant**: **Amended again 2026-08-13** (`check_completeness_schema`): `VERDICT_SCOPES` re-exported and added to `__all__`, both in leading position (an upper-case name sorts before the class names). Additive again. **Amended again 2026-08-18** (`confidence_vocabulary_single_definition`): `CONFIDENCES` re-exported and added to `__all__`, both in leading position, ahead of `VERDICT_SCOPES`. Additive again — and the second module constant in a row, so the note above holds in the form it was written: **a new module constant means this file changes** **Amended again 2026-08-21** (`three_field_vocabularies`): three more module constants re-exported and added to `__all__` — `ELEMENT_ROLES` and `SOURCE_REF_KINDS` from `stack`, `SUBJECT_KINDS` from `spec_library` — all in leading position among the upper-case names. Additive again, and the **third** handoff in a row to add a constant, which is now simply what closing a comment-defined vocabulary does here |
| `tolerance_stack/stack.py` | same path | **yes, since 2026-08-04** (`pitch_link_stack`) — was byte-identical at import. **Comment only:** `SourceRef.kind`'s inline list gained `spec`, which the SOP already mandates for a `data/inbox/specs/` file and which the imported comment omitted. No executable line changed; `fold()` is untouched and still reads `min`/`max` only. **Amended again 2026-08-05** (`sop_edits_apply`), also **comment only**: `StackElement.role`'s inline list gained `nut_geometry`, which the seeded take-2 has used three times since the import and which the SOP already documented. **Amended again 2026-08-05** (`hub_bearing_thermal_stack`) — and this one **does change executable lines**, the first time since the import: `Term` gained a positive `coefficient` (default `1.0`) plus a `weight` property, and `fold()`, `terms()` and `_expand()` use it. Behaviour-preserving by construction (default weight is the old `±1`) and verified so: the three seeded stacks re-derive against the 260729 workbook with a largest delta of **6.439e-15**, the same as before. `fold()` is still the only place element values are combined and still never reads `lmc`/`mmc` — a test now asserts the latter by reading the function's own source. Rationale in ARCHITECTURE.md, "Where computation may live — and the coefficient". **Amended again 2026-08-06** (`citation_export_provenance`), **executable**: the new frozen dataclass `SourceExport` (which export of a document a citation was read from, identified by `sha256`) plus a validating `__post_init__`, an `export` field on `SourceRef`, and `SourceRef.from_dict` inflating a nested dict into it. Additive to the citation shape — no existing field changed meaning, and `fold()` is untouched, still the only combiner and still reading `min`/`max` only. Row added during `review/citation_export_provenance`. **Amended again 2026-08-07** (`readonly_invariant_evidence`), **executable**: the frozen dataclass `ExportRun` (`run_id` + the `ts` from that run's `run_meta.json`), which `SourceExport.runs` is now a sequence of, plus a `run_ids` convenience property and normalisation in `SourceExport.from_dict`. The **one non-additive edge**: a bare run-id string — the shape written the day before — now raises rather than being accepted, because a run id with no `ts` is the state in which the read-only invariant cannot be checked at all, and silently accepting both shapes would let the next stack write the old one. `fold()` untouched again. **Corrected during `review/readonly_invariant_evidence`, comment only:** `ExportRun`'s note keyed "this `ts` was derived from the run id" on `run_meta.json`'s `backfilled: true`, which misses the two unflagged 2026-04-09 runs of 215197 — the tell is the whole-second shape that spells the id back, not the flag. Same correction in the SOP and the session lesson. **Amended again 2026-08-13** (`check_completeness_schema`), **executable**: `CheckResult` gained `complete: bool = True` and `excluded_terms: Sequence[str] = ()` with a `__post_init__` enforcing the invariant in **both** directions (`complete: false` with no terms raises; terms with `complete: true` raises), a derived `verdict_scope` property (`joint` | `budget`) and both fields plus the scope in `as_dict()`; `StackDefinition.check()` reads the two keys off the check spec with those defaults; the module constant `VERDICT_SCOPES` was added. **`verdict`'s `pass|marginal|fail` domain is deliberately untouched** — the honesty about an incomplete check lives in the scope, so no existing consumer of `verdict` changed. Additive for every stack authored before the fields existed (both defaults are the old behaviour) and `fold()` is untouched again, still the only combiner and still reading `min`/`max` only. This is what let `build_viewer_projection.is_incomplete`'s prose search be deleted — see `docs/issues/ISSUE_20260805_check_result_has_no_complete_flag.md`. **Amended again 2026-08-18** (`confidence_vocabulary_single_definition`): the module constant `CONFIDENCES` was added and `SourceRef` gained a `__post_init__` that validates `confidence` against it — **the first behaviour change to `SourceRef` since the import, and it is a narrowing, not an addition**: `SourceRef(kind="drawing", confidence="banana")` constructed happily before and now raises. Nothing on disk moved (all 48 live element citations and all 11 filled `values_source` blocks already spell one of the three words, and the field default `"untraced"` is a member — the `values_source` figure read **9** until `review/confidence_vocabulary_single_definition` recounted it against `hardware_entries.json`: 15 entries, 11 with a filled `values_source`, 4 `not_transcribed` with none), so no stack file, no hardware entry and no projection byte changed — but a *loader* that used to accept a typo does not any more, which is the point: the vocabulary used to be an end-of-line comment on the field (`# traced | inferred | untraced`), which is why three independent copies of it had accumulated across the repo and why `apps/viewer/viewer.js`'s `VA.CONFIDENCES` could not be paired against anything. That comment is deleted; the tuple is the definition and `spec_library.CONFIDENCES` and `build_viewer_projection.CONFIDENCE_ORDER` now read it. `fold()` is untouched again. See `docs/issues/ISSUE_20260812_the_confidence_vocabulary_has_no_single_definition_to_pair_va_confidences_against.md` **Amended again 2026-08-21** (`three_field_vocabularies`), **executable, and the same narrowing twice more**: the module constants `SOURCE_REF_KINDS` and `ELEMENT_ROLES` were added, `SourceRef.__post_init__` gained a `kind` check and `StackElement` gained a `role` check (its `__post_init__` previously enforced only `min <= max`). Both end-of-line pipe-list comments are deleted; the tuples are the definitions. `SourceRef.kind`'s check is **the first one that ever existed** — `docs/SOP_TOLERANCE_STACK.md` had told authors since 2026-08-04 that a new kind must reach "all three" places "or the SOP is describing something the suite rejects", and the suite rejected nothing: the test whitelist only walked the elements on disk. Nothing on disk moved (all 48 live element citations already spell one of the six kinds, all 48 elements one of the eight roles, and so do the 11 filled `values_source` blocks), so no stack file, no hardware entry and no projection byte changed — but as with `confidence` on 2026-08-18, a loader that used to accept a typo does not any more. `fold()` is untouched again. **One further line, comment only:** `SourceExport.status`'s `# established | unestablished` became `# one of EXPORT_STATUSES, above` — that constant has existed and been enforced since 2026-08-06, so the pipe list beside it was a second source of truth, and a *two*-word vocabulary has only one pipe, which is why the sweep for comment-defined vocabularies had never found it. See `docs/issues/ISSUE_20260818_three_more_field_vocabularies_are_defined_by_a_comment.md` |
| — (new) | `tolerance_stack/thermal.py` | **not imported** — written 2026-08-05 by `hub_bearing_thermal_stack`. The thermal-fit archetype layer: material entries, `thermal_factor()`, and the check generator. Nothing in drawing-checker corresponds to it |
| — (new) | `tolerance_stack/spec_library.py`, `tolerance_stack/__main__.py` | **not imported** — written 2026-08-05 by `spec_library_v0` |

Stdlib only. `SCHEMA_*` constants keep the `joby.tolerance_stack/...` namespace
they were minted with — the schema id is not repo-scoped, so moving repos does
not rev it.

## Copied — tests and the four debug tools

| source | destination | amended |
|---|---|---|
| `tests/test_tolerance_stack.py` | same path | **yes, since 2026-08-04** (`pitch_link_stack`) — was byte-identical at import (**34 tests, all green** under this repo's `venv-win`). Added the pitch-link stack's value and provenance tests, hoisted the stack-file list into `ALL_STACK_FILES`, and admitted `spec` to the `source_ref.kind` whitelist. **50 tests** as of that commit (51 after the review added one). SOP Step 7 requires new tests per stack, so this file changes with every new stack. **Amended again 2026-08-05** (`sop_edits_apply`): three tests for the `values_source` requirement and for Step 5b's transitive workbook ban, plus a parametrized `role`-vocabulary test. **58 tests** at the end of that handoff. **Amended again 2026-08-05** (`spec_library_v0`): the two tests asserting `library_ref is None` on every entry became one general invariant — a filled ref means `values_status == "library"`, a null ref means it does not. Count unchanged at **58**. During `review/spec_library_v0` one further guard was narrowed, where `sop_edits_apply`'s "non-inline implies no `values_source`" collided with that promotion; see that review report. **Amended again 2026-08-05** (`hub_bearing_thermal_stack`): six `Term.coefficient` tests, a test that reads `fold()`'s own source to assert it still contains no `.lmc`/`.mmc`, a counts-match-the-description test for `hardware_entries.json`, both thermal stacks wired into `ALL_STACK_FILES`, and `test_hardware_entries_flag_the_two_parts_missing_from_the_assembly` sharpened -- it collapsed `present: False` (a design finding) with `present: None` (not checked) and would have manufactured a finding for the two new bearing entries. **71 tests** in this file, and **193** across the suite, at the end of that handoff (the other 122: 12 + 36 in the two new `test_hub_bearing_*` files, 44 + 30 in `spec_library_v0`'s two). **Amended again 2026-08-06** (`traced_labels_and_ratio`): six tests for the `traced` label and the ratio built out of it — `test_no_traced_element_cites_a_parts_list` (parametrized over every stack) and its `hardware_entries.json` twin, the two re-cited grips pinned to the printed sheet-3 cells, the MS21299 washer pinned to `inferred`, `test_the_seeded_traced_ratio_is_the_number_every_document_quotes` (which imports `debug_report_tolerance_stacks._counts` so prose, command and test cannot drift in three directions), and a doc-level test that fails when a live doc quotes a stale ratio. Row amended during `review/traced_labels_and_ratio`. **Amended again 2026-08-06** (`citation_export_provenance`, the sibling handoff that landed on `master` mid-review): five tests for `source_ref.export` — the mandatory-export invariant over `ALL_STACK_FILES`, the "no `unestablished` export written as a concrete one" guard from both sides, the 64-hex `sha256` requirement, and one asserting the *non*-decision that `element_id`/`run_id` stay null rather than absorbing the export. Row added during `review/citation_export_provenance`. **The two sets met in that second review's merge commit**, and the counts here are the only ones measured on a tree where both exist: **97 tests** collected from this file and **277 passed / 1 skipped** across the suite (`test_viewer_crops.py`, not an imported file, contributes 37). One assertion had to move in that merge: `test_the_export_is_a_sibling_of_the_feature_identity_slot_not_a_filling_in` hard-coded **25** backfilled exports, and the two grips re-cited to `kind: "spec"` are exempt from the export requirement, so the true count is **23** — two blocks correctly dropped, each element's `note` recording the sha256 that was on it. Neither branch's suite could see that; only the merge could. **Amended again 2026-08-07** (`readonly_invariant_evidence`): eight tests for the run-`ts` shape and for the invariant it exists to make checkable — `test_every_cited_run_carries_the_ts_from_its_own_run_meta` (parametrized over every stack, and it demands a *tz-aware* stamp, since a naive one cannot be compared with a commit time), `test_a_bare_run_id_is_refused_because_a_name_is_not_an_identity`, and `test_the_pitch_link_stacks_cited_runs_predate_that_sessions_first_commit`, which is the first place in this repo where "nothing was written into drawing-checker" is *verified* rather than asserted: it pins both cited runs against `d6829f2` (that session's first commit) and `20260803_145243` against the root commit `e7bd996`. Its two constants are git history, which does not move — the failure mode it guards is a citation gaining a run that postdates the session. **105 tests** in this file, **308 passed / 1 skipped** across the suite, both measured **in the worktree** (the main checkout reports one more passed and none skipped — one test is data-dependent and skips where `data/` is empty). **Amended again 2026-08-10** (`fastener_citations_and_confidence`): two tests added and five changed. Added `test_a_workbook_only_value_is_untraced_unless_its_exception_is_registered` (parametrized over every stack — the SOP's *"a value whose only support is 'the source workbook says so' is `untraced`"* rule, written as an **allowlist** rather than an implication, because the implication is false: `hub_bearing_thermal_fit_m1`'s two hub bores cite a workbook and are correctly `inferred`, their support being a later revision of the same part drawing named in their notes) and `test_the_workbook_allowlist_has_no_dead_entries`, so a registered exception that stops existing cannot silently unguard an id. Changed: the re-cited-grips test grew from two rows to four and gained an assertion that the two `fastener_grip_13` instances have **identical** citation shape, which is what stops the same bolt drifting into two stories again; the seeded and all-stack ratio pins moved to 5/3/18 of 26 and 21/7/20 of 48; `hardware_entries.json`'s count pins moved with the file. And `test_the_export_is_a_sibling_of_the_feature_identity_slot_not_a_filling_in` **lost its hard-coded total of 23** — its own docstring had said that a cross-stack total is a cross-handoff coupling and to replace it with the invariant if it churned again; it churned again (23 → 22, `fastener_grip_13` becoming `kind: "spec"`), so the instruction was taken and the test now asserts the invariant in both directions instead. **Amended again 2026-08-11** (`sop_library_ref_pairing`), **no test added or removed and no number changed**: `test_every_hardware_entry_has_a_gap_list_and_a_resolvable_values_status`'s body was extracted into a module-level `hardware_entry_problems(entry)` returning complaints, so that the SOP's own Step 4 examples can be run through **the same** rules as the seeded entries by the new `tests/test_sop_vocabulary.py` (not an imported file, so it has no row here). The function is a strict superset of what the test asserted before — it also requires the `library_ref` and `values_source` keys to be *present* — and the test's docstring lost its "the remaining twelve are still `inline`" clause, which was written against a 13-entry file and had been false since the two bearings landed on 2026-08-05. **Amended again 2026-08-12** (`hardware_counts_doc_guard`), **two tests added, none removed, no existing assertion or number changed**: `test_no_live_document_states_an_unguarded_hardware_entry_count` walks every live `.md` in the repo plus the `.json` under `docs/` — `docs/sessions/`, `docs/issues/`, `docs/reference/` and this file are excluded as dated history — matches the count-claim *shapes* this repo has actually written, and recounts each against `hardware_entries.json`, so a stale copy fails the suite wherever it lives instead of only where the last one was found. A number inside a blockquote or a double-quoted phrase is exempt, which is what lets the repo's "correct in place, leave the old one visible" convention survive the guard. `test_the_hardware_entry_count_guard_can_fail` replays the README sentence that went stale and asserts the scanner flags it, and stays silent on that same sentence quoted as a correction. The scan also picked up a previously unguarded claim inside `NAS6403U11D`'s `library_ref_note` (*the other N do not defer, M inline / K not_transcribed*), which is now recounted too. `test_hardware_entry_values_source_counts_match_the_description` is untouched and still owns the one-file pin. **Amended once more during `review/hardware_counts_doc_guard`**, one assertion inside `test_the_hardware_entry_count_guard_can_fail` and nothing else: its second assertion pinned the replayed stale sentence's digits against the live counts (`== [8, 3, 1]`), so adding a single drawing-sourced entry -- which this file does with every new stack -- failed it with a bare `[8, 11, 3, 1, 2] == [8, 3, 1]`. Narrowed to the count *keys* the sentence got wrong (`workbook`, `safe`, `spec`), which is the durable claim; the denominator was legitimate on 2026-08-12 and need not stay so. **No scan shape, no count, no other test and no other assertion changed**. **Amended again 2026-08-13** (`check_completeness_schema`): **eight tests added, none removed, one existing assertion replaced.** The eight are a section on completeness as a schema field — the defaults, the budget scope with `verdict`'s domain unchanged, the bidirectional invariant from **both** sides, the empty-string guard on an excluded term, the ride-through from a check spec (the same dict an archetype loader builds), and `as_dict` carrying field and derived scope. The replaced assertion is in `test_pitch_link_shank_out_deficit_is_the_required_link_eye_width`: `assert "INCOMPLETE" in got.label` became `complete is False` / `verdict_scope == "budget"` / the excluded term / `"INCOMPLETE" not in got.label`, because the pitch-link stack's two checks were migrated off the shouted label suffix in the same commit. **121 tests** in this file, **374 passed / 1 skipped** across the suite, measured in the worktree with the main checkout's interpreter (the one skip is `test_viewer_js_suite`'s node-fs tier, which has no `data/` here). **Amended again 2026-08-17** (`traced_ratio_guard_freshness`), **one test added, none removed, one existing test's stale-figure half generalised; no count, no value and no other assertion changed**: `test_every_document_quoting_the_traced_ratio_quotes_the_current_number` knew **one** superseded ratio (as the bare substring `"of 17"`) and exempted it only inside a markdown blockquote, so `3 of 26` — retired 2026-08-10 and still written in seven live documents (eight occurrences, one of them bare) — was unguarded, and `LESSONS_20260810_fastener_citations_and_confidence.md` had already flagged that it was missing. The figure list is now `_RETIRED_TRACED_RATIOS`, holding both retired ratios with the handoff that retired each, matched by a pattern anchored on **both** numbers (the denominator-only substring flagged a correct `4 of 17` and could not see any figure sharing 26 with the live one), and the exemption routes through `_quoted_spans()` — blockquote **or** double-quoted phrase — which `hardware_counts_doc_guard` added on 2026-08-12 for the same problem in JSON. That function **moved up** in the file, unchanged, to sit above the first of its two callers; it is now visibly one definition serving both doc-level scans rather than two notions of "quoted" in one file. The `missing` half is unchanged except for its message, which now tells the handoff that moves the ratio to append the figure it retired — that handoff is the only one that fails this half, and it was the step nobody had a prompt for. Added `test_the_traced_ratio_guard_can_fail` (the newly-covered figure as a bare claim, then the same sentence blockquoted and double-quoted, plus the older figure in the wordier `1 traced out of 17` form the worksheets use) and `_current_traced_ratio()`, which replaced the inline comment `# "3 of 26"` beside the line that computes the live figure — a cached copy of a number one line from its own recount, which is the defect this whole guard exists against, and the reason this handoff was filed. The generalised scan found **one live bare claim**, `docs/SOP_TOLERANCE_STACK.md:96`; that sentence's retired figure is now quoted rather than deleted. **Amended once more during `review/traced_ratio_guard_freshness`**, `_retired_ratio_pattern` and two assertions added to `test_the_traced_ratio_guard_can_fail`, and nothing else -- **no test added or removed, no count, no value, no scan scope and no other assertion changed**: the pattern's 40-character wildcard was anchored on the first number alone, so it read the repo's own long form (*N traced / M inferred / K untraced, out of T element instances*, the shape the review checklist asks every report to state) and matched on the **inferred** column -- the current figure written long, *5 traced / 3 inferred / 18 untraced, out of 26*, was flagged as the retired *3 of 26*, which is the guard firing on the one number it exists to protect. The wildcard is now reachable only behind the literal word `traced`, so the numerator must be the traced column; every positive case still fires, including the retired figure in that same long form, and the two added assertions pin both directions with the negative one built from `_RETIRED_TRACED_RATIOS` so it can neither go vacuous nor go stale. **Amended again 2026-08-18** (`confidence_vocabulary_single_definition`), **one test added, none removed, no count and no value changed; two existing assertions stopped re-listing a vocabulary**: `test_a_source_ref_refuses_a_confidence_outside_the_vocabulary` pins the new `SourceRef.__post_init__` from four sides — a plain misspelling, the empty string, a case variant, and `"no_source_ref"` (the value a reader is likeliest to think is legal, because the *viewer* renders it as `NO CITATION`; it is minted by the projection for an element that has **no** `source_ref`, so a `SourceRef` spelling it would be a citation asserting it does not exist) — plus `from_dict`, which is the path every stack file takes, and the field default, which must be a member or every citation written without one would raise. The two changed assertions are `test_every_element_carries_a_source_ref_with_a_confidence` and `test_every_inline_hardware_entry_cites_where_its_values_came_from` (this row and the lesson both named `test_every_hardware_entry_has_a_gap_list_and_a_resolvable_values_status` — a different test, forty lines further down, which was never touched; corrected in `review/confidence_vocabulary_single_definition`): each spelled `in ("traced", "inferred", "untraced")`, a third and fourth copy of the vocabulary living in the tests, and each now reads `in CONFIDENCES` imported from the package. The second is *not* redundant with the dataclass check — it reads `hardware_entries.json` as raw JSON and never constructs a `SourceRef`. **124 tests** in this file, and across the suite **441 passed / 1 skipped in a worktree**, **442 passed / 0 skipped in the main checkout** — the skip is the viewer's data-dependent tier, and this row said only the first figure until `review/confidence_vocabulary_single_definition` ran both and named the checkouts. **Amended again 2026-08-21** (`enumerated_state_doc_guard`), **two tests added, none removed, no existing assertion or number changed**: closes `ISSUE_20260812_the_doc_scan_guards_cannot_fail_on_a_deleted_section.md` (shape 2 of `BRIEF_20260817_doc_scan_deletion_guards.md`, staged 2026-08-19). `test_every_enumerated_viewer_state_is_named_in_a_live_document` asserts every state `VA.EXPORT_STATUSES` (`tolerance_stack/stack.py`) and the `values_status` check (`tolerance_stack/thermal.py`, read via `tests.test_js_python_vocabulary.python_values_statuses` rather than a third copy of the list) can produce is named by its code spelling somewhere in `apps/viewer/README.md`, found through `live_documents()` rather than a bare path open. `test_the_enumerated_state_doc_guard_catches_the_08_12_deletion` replays, on an in-memory copy of the README, the exact edit `hardware_counts_doc_guard`'s stale-count guards above could not see — cutting `## Which bytes the number was read off` plus the `EXPORT UNESTABLISHED` / `CTE NOT TRANSCRIBED` legend rows — and asserts the new guard goes red on it. **126 tests** in this file, **443 passed / 1 skipped** across the suite in the worktree (the skip is the viewer's data-dependent tier). | **Amended again 2026-08-21** (`three_field_vocabularies`), **two tests added, none removed, no count and no value changed; three existing assertions stopped re-listing a vocabulary and two fixtures stopped carrying a role that is not one**: the two are `test_a_source_ref_refuses_a_kind_outside_the_vocabulary` and `test_a_stack_element_refuses_a_role_outside_the_vocabulary`, each pinning its new `__post_init__` check in **both** directions — every word in the constant constructs, and a word outside it raises, so the constant and the validator cannot become two facts — plus the `from_dict` path, which is what `load_stack` uses for every element in every file. The three changed assertions are in `test_source_ref_leaves_the_feature_identity_slot_open_and_empty`, `test_every_inline_hardware_entry_cites_where_its_values_came_from` (both spelled the six kinds out longhand) and `test_element_role_comes_from_the_documented_vocabulary` (the eight roles); each now reads the constant. The two fixtures are `_el()` and `test_element_rejects_inverted_limits`, which built elements with `role="test"` — harmless while nothing checked, and exactly what the check now refuses; both use `clamped_member`, the most ordinary word in the vocabulary, and no assertion in either depends on the role. **126 tests** in this file and **447 passed / 1 skipped in a worktree** on this branch alone -- but `enumerated_state_doc_guard` landed on `master` two tests into the same file first, and only the merge sees both: **128 tests** in this file, **450 passed / 0 skipped in the main checkout** on the merged tree (no data-dependent skip there). Recomputed during `review/three_field_vocabularies`. **Amended again 2026-08-25** (`fastener_stack_shadow`): a fifth stack fixture and five new tests for `stack_rotor_fastener_length.json` (the grip family, the sourced-clamped-stack zero-width path, the nine per-dash budget checks, the no-workbook/zero-width-band declarations, and the no-castellation confirmation), `stack_rotor_fastener_length.json` added to `ALL_STACK_FILES`, the seeded-vs-all-stacks ratio's "every" half moved 21/48 -> 30/59 (the seeded half, scoped to the three original slice-1 stacks, is untouched), and `test_hardware_entry_values_source_counts_match_the_description` / `test_every_inline_hardware_entry_cites_where_its_values_came_from` updated for the ten new `hardware_entries.json` rows. **Amended again 2026-08-27** (`material_values_status_vocabulary`), **one test added, none removed, no count or value changed; one existing check's literal replaced with a shared name**: `hardware_entry_problems()`'s `values_status` membership test, previously its own copy of the three words, now reads `tests.test_js_python_vocabulary.python_values_statuses()` — the same AST-read `MaterialEntry.__post_init__`'s own check is compared against — so `hardware_entry` and `MaterialEntry` share one definition of the vocabulary instead of two that happen to agree today. Added `test_no_persisted_field_vocabulary_is_an_inline_literal`, the generalized form of the "is there a fifth [enumerated field vocabulary]?" question `three_field_vocabularies` (2026-08-21) asked by hand: it AST-scans every `self.<attr> {in,not in} (...)` check in `tolerance_stack/` and fails if the right-hand side is an inline literal of two or more string constants rather than a named constant — scoped to `self.<attr>` specifically so it does not flag `thermal.py`'s `corner`/`stage`/`group` locals, which are labels, not persisted fields, and were already ruled out by hand in the lesson this generalizes. **142 tests** in this file, **473 passed / 1 skipped** across the suite in the worktree with the main checkout's interpreter (the skip is the viewer's data-dependent tier). **Amended again 2026-09-01** (`claude_md_tracked`), **no test added or removed, no count, no value and no assertion changed; one scan-scope constant narrowed**: `_HISTORICAL_NAMES` dropped `CLAUDE.md`, which it had excluded from `live_documents()` with the note *"gitignored, per-session"*. That file is tracked as of this handoff and states what is true now rather than what someone believed on a date, so the two doc-level scans that walk `live_documents()` -- the hardware-entry count guard and the enumerated-state surface lookup -- now read it like any other live document. `PROVENANCE.md` stays on the list, for the reason the comment above it gives. **142 tests** in this file, unchanged, and **559 passed / 1 skipped** across the suite in the worktree with the main checkout's interpreter (the skip is the viewer's data-dependent tier). |
| `tests/__init__.py` | same path | no — empty, both |
| `tests/debug_dump_tol_stack_xlsx.py` | same path | **yes** — import note; dropped a "drawing-checker's venv-win" aside; folded in the shared-formula gotcha |
| `tests/debug_report_tolerance_stacks.py` | same path | **yes** — import note. **Amended again 2026-08-06** (`traced_labels_and_ratio`), and this one adds **executable lines**, the first time since the import: a `--ratio` mode (`SEEDED_STACK_FILES`, `_counts()`, `ratio()`) that is now the single place the repo's traced/inferred/untraced ratio is computed. Every document quoting the ratio points at it and `test_tolerance_stack.py` imports `_counts` rather than re-implementing the arithmetic. The three pre-existing modes are untouched. Row amended during `review/traced_labels_and_ratio`, which found it still describing an import-note-only change |
| `tests/debug_stack_hardware_crosscheck.py` | same path | **yes** — import note: the `data/runs/` it reads is drawing-checker's, plus the `item_no` / `find_no` key mismatch |
| `tests/debug_trace_stack_values.py` | same path | **yes** — import note: needs PyMuPDF (deliberately absent from `requirements.txt`) and a drawing-checker PDF |

The test file needed **no** path edits: it resolves `docs/tolerance_stacks/` as
`Path(__file__).parent.parent / "docs" / "tolerance_stacks"`, and this repo
reproduces that layout exactly.

Two of the four tools are cross-repo by nature (`debug_stack_hardware_crosscheck`
wants a drawing-checker run dir; `debug_trace_stack_values` wants a drawing PDF
and `fitz`). They were imported anyway because they are how a `source_ref` gets
its `traced` confidence — see the SOP's tracing step.

## Copied — the source workbook

| | |
|---|---|
| source | `C:\workspace\drawing-checker\data\inbox\tolerance_stacks\260729_sample_tol_stack.xlsx` |
| destination | `data/inbox/tolerance_stacks/260729_sample_tol_stack.xlsx` |
| sha256 | `51b6c5362848758aaeebd8281f96e1ba4786abbeb40642b94e7f98bffecd6fd1` — **verified identical after the copy** |
| size | 113,156 bytes |
| ultimate origin | forge atomic-note attachment `20260729T173648_qjk2xk` (immutable) |

Contents gitignored, `PROVENANCE.md` committed — the same rule the original
carried. `data/inbox/tolerance_stacks/PROVENANCE.md` was copied and extended with
this second hop.

## Copied — the slice-1 lesson

| source | destination |
|---|---|
| `docs/sessions/lessons/LESSONS_20260729_tolerance_stack_slice1.md` | `docs/reference/LESSONS_20260729_tolerance_stack_slice1.md` |

The imported text is intact. Two blocks have been **inserted** into it, and
nothing has ever been deleted or reworded — `docs/reference/` is **insert-only**,
the rule and its argument being in ARCHITECTURE.md, "Imported material", and
`tests/test_provenance.py` diffing this file against drawing-checker's blob at
the sha above and failing on any opcode that is not an insertion. It is the
primary source behind the SOP and the review checklist.

| inserted | what |
|---|---|
| at founding (2026-08-03) | the header marking it imported reference, stating that its paths are drawing-checker-relative, and naming the one part of it this repo supersedes (its "leave the stacks where they are through phase 2" recommendation) |
| 2026-08-06 (`traced_labels_and_ratio`) | a 30-line dated `CORRECTION` blockquote, *after* — not instead of — the paragraph this repo's wrong "1 of 17" traced ratio originated in. The original sentence is left standing so the mistake stays legible; the block states the corrected figure (3 traced of 26 element instances), how each half of the old one went wrong, and where the ratio's definition now lives. Row added during `review/traced_labels_and_ratio`, which found this section still claiming verbatim |

**Amended 2026-08-10** (`provenance_byte_identical_test`): the header block's
*"verbatim, do not edit"* was the rule that made the 2026-08-06 insertion an open
question — `review/traced_labels_and_ratio` let it stand and flagged the
contradiction in
`docs/sessions/reviews/REVIEW_20260806_traced_labels_and_ratio.md` (finding S1).
Settled in favour of insert-only rather than reverting a true correction; the
header now states that rule and points at the test. **No imported line was
touched by that edit either** — it is a rewrite *within* the inserted header.

## MOVED — the spec/datasheet pile

The only thing that did not stay in drawing-checker, and the founding handoff's
one sanctioned change outside this repo.

| | |
|---|---|
| source | `C:\workspace\drawing-checker\data\inbox\specs\` |
| destination | `C:\workspace\tolstack\data\inbox\specs\` |
| moved | 2026-08-03 |
| verified | **42 files, 111,575,456 bytes** on both sides of the move (the count includes hidden `desktop.ini`, which a `Get-ChildItem` without `-Force` misses) |
| breadcrumb | `MOVED_TO_TOLSTACK.txt`, one line, left in the old location |

Moved rather than copied because nothing in drawing-checker had ever read it, and
because it is the *trace target* for stack elements — it belongs with the stacks
that cite it. **Append-only from now on**; see `data/inbox/specs/README.md`.

Those two numbers pin the **move**, not the current contents. The pile has since
grown — **64 files / 249,106,379 bytes**, re-counted 2026-08-05 by
`hub_bearing_thermal_stack` — which is append-only working as intended, not a
falsified row. Count it before quoting it.

**Correction, 2026-08-05** (`hub_bearing_thermal_stack`): the previous wording of
this paragraph said the pile included "`MS9363 Rev C.pdf` and five hub-bearing
drawings". `MS9363 Rev C.pdf` is there; the **five hub-bearing drawings are
not** — Jeff dropped those into drawing-checker's
`data\inbox\drawings\`, and they were copied into this repo's *own* new
`data/inbox/drawings/` stream, not into `specs/`. An `ls` of `specs/` returns no
`212966` / `214955` / `214959` / `214588` / `214589`. The byte count in the same
sentence was also 488 bytes stale. Both halves were written one day before the
drawings existed here, which is the recurring-bug class the review checklist
names: **an inventory number or contents claim written from expectation rather
than from a count.** Recount, don't read.

Note for anyone auditing drawing-checker's data tree: that folder was
**untracked**, not gitignored. The founding handoff described both sides as
gitignored; the destination is, but drawing-checker's `.gitignore` uses per-stream
patterns and never covered `specs/`. No drawing-checker commit was involved
either way.

## Not imported

Deliberately left behind, because they are drawing-checker's own and this repo
has no use for them: `parser/`, `pipeline/`, `webui/`, `scoring/`, that repo's
`conftest.py` (it exists to redirect the pipeline's data root — there is no
pipeline here), its `requirements.txt` (PyMuPDF, pdfplumber, pandas, FastAPI —
none needed), and the other ~35 `tests/debug_*.py` tools, which are parser and
web-UI diagnostics unrelated to stacks.
